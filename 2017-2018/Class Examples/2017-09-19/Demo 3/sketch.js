function setup() {
  createCanvas(500, 500);

  strokeWeight(5);

  var y1 = 0;
  var y2 = 500;

  var x = 0;

  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
  line(x, y1, x - 50, y2);
  x = x + 10;
}