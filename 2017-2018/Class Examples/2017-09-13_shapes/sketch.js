function setup() {
  createCanvas(600, 600);

  fill(255, 255, 0);
  rect(30, 40, 50, 60); //Draw a rectangle (x pos, y pos, width, height)
  ellipse(170, 80, 10, 20); //Draw an ellipse (x pos, y pos, width, height)
}